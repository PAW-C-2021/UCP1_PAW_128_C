@model IEnumerable<Pendaftaran.Models.CustomerService>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaCustomerService)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JenisKelamin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaCustomerService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JenisKelamin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCustomer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCustomer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCustomer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
