@model IEnumerable<Pendaftaran.Models.KepalaSekolah>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaKepalaSekolah)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaKepalaSekolah)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKepalaSekolah">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdKepalaSekolah">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdKepalaSekolah">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
